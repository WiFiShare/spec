# Changelog

## Unreleased (draft `1`)

- First draft of the data model, schemas, privacy rules and OpenAPI document.
- Filter rules R1–R12 with acceptance fixtures.
- Publish rules P1–P8.

Nothing here is frozen. Wire formats get a new schema id when they change in a
way that breaks a released client.
